uniform+ settings ABLATION sweep (tune_5d_hsm.R, MODE=grid) -- for the appendix.
  grid: environmental.cutof.percentile in {0.001,0.005,0.01,0.05,0.1,0.15,0.2,0.25}
        x species.cutoff.threshold   in {1.0,0.9,0.75,0.6,0.5}
        = 40 cells. species.cutoff.threshold = 1.0 is the PURE-UNIFORM endpoint
        (presence GMM disabled; USE.MCMC paSamplingMcmc samples the environment
        uniformly -- commit 6903ab1). HIGHER species_cutoff = WEAKER presence
        exclusion; 1.0 is the continuous limit (no exclusion), not an inversion.
  fixed: 4 species, 25 Bernoulli realisations, pc5 predictors, chain 20000,
         burnIn 1000, max_pres 300, bgk_prev 1.
  speedups A1-A4 (uniform+ only / no hypervolume / pc5 / 25 reals);
  RND+buffer baseline = results/5d_hsm (RND cor_truth 0.759, buffer 0.725).

  Run: job 4847339, 40 cells x 4 species = 160 jobs on 64 cores, 10m55s.
       20000 rows, all status=ok.
  Reproduce (commas in the value -> set the vars in the env, --export=ALL):
    cd $HOME/GaussNiche
    MODE=grid ENV_CUTOFFS="0.001,0.005,0.01,0.05,0.1,0.15,0.2,0.25" \
      SPECIES_CUTOFFS="1.0,0.9,0.75,0.6,0.5" \
      sbatch --export=ALL sbatch/submit_tune_5d_hsm.sh
  Files: summary_tune_grid.rds ($tune 20000 rows / $cell 40 medians / $ref baseline),
         tune_cell_medians_grid.csv (40 rows), tune_heat_<metric>_{value,delta}_grid.pdf,
         tune_hsm_metrics_5d_grid.csv (raw, GITIGNORED -- regen from $tune if needed).

  REGEN NOTE (supersedes the pre-b716c0c grid): the recent USE.MCMC rebuild
  (=1/unify/CRAN, a57e34e->HEAD) did NOT change the sampler mechanism -- PROVEN
  byte-identical: rebuilding a57e34e (0.0.4, pre-=1) and diffing vs HEAD (0.0.5)
  gives identical pseudo-absences (same points, same env GMM densities/threshold) at
  env 0.05 and 0.2; env5d + packages unchanged. The pre-b716c0c grid is NOT
  seed-for-seed reproducible only because it was generated MID-DEVELOPMENT (jobs ran
  Jun 26 before the build/RNG state was finalized+committed, =1 landed hours later),
  so it carries a different random STREAM, not a different mechanism -- per-cell
  medians still match the clean grid within 0.02. The current 40-cell grid is clean
  and reproducible within the build (env<=0.15 sub-grid re-ran bit-for-bit, max |diff| 0).

  DEGENERACY: onset is ~0.25-0.3 and borderline/stochastic -- and ALWAYS was in the
  committed mechanism. The clean code (a57e34e=HEAD) samples env=0.2 robustly (proven:
  300 pts at env=0.2, byte-identical old vs new). The old "env=0.2 degenerate" note
  was a DEVELOPMENT-BUILD artifact, not a property of the committed code -- disregard.
    env=0.2  : valid, low fallback (probe 10/10 at sp=1; grid ~4% fallback rows).
    env=0.25 : SOFT degeneracy -- ~24% of (species x realisation x algo) units
               degenerate and FALL BACK to pa_random (probe 9/10 at sp=1; grid 24%).
    env=0.3  : ~20% degenerate at sp=1 (probe); not in the committed grid.
  Random-fallback fraction by env (units identical across ALL 5 species_cutoff incl
  uniform=1, i.e. the knob-blind random draw): 0.001 1% / 0.005 0 / 0.01 1% /
  0.05 0 / 0.1 0 / 0.15 4% / 0.2 4% / 0.25 24%. The ~1% baseline at low env is
  sparse-presence rare-species realisations, env-independent.
  *** FLAG: the env=0.25 row is materially fallback-contaminated (24%); its
  cor_truth (~0.72-0.73) is INFLATED toward RND (random fallbacks recover the true
  surface better). The genuine-uniform value at 0.25 is lower. Treat 0.25 as an
  upper bound. env<=0.2 is essentially clean. ***

  CONCLUSION (unchanged, strengthened): environmental.cutof.percentile is the lever
  -- raising it 0.001->0.25 lifts cor_truth ~0.62->~0.72 monotonically (diminishing
  returns). species.cutoff.threshold is a NON-lever: flat across {0.9..0.5} AND at
  the pure-uniform endpoint 1.0. Even pushed to the practical ceiling (~0.2-0.25),
  uniform's truth-recovery only reaches buffer-out's level (~0.72 vs 0.725) and
  stays BELOW RND (0.759) -- and past ~0.2 the apparent climb is partly random-
  fallback artefact. The truth-recovery gap is structural to environmental-uniform
  sampling and cannot be tuned away with these two knobs.
